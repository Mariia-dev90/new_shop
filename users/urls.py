
from django.urls import path

# urlpatterns = [
#     path('register_purchaser/', RegisterPurchaserView.as_view(), name='register_purchaser'),
#
#     path('login_purchaser/', LoginPurchaser.as_view(), name='login_purchaser'),
#     path('restore_access_purchaser/', Restore_access.as_view(), name='restore_access'),
# users/urls.py

from django.urls import path

urlpatterns = [
    # Пока ничего нет, но можно добавить тестовый путь
    # path('test/', some_view),
]
